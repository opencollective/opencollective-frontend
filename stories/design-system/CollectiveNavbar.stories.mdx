import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import CollectiveNavbar from '../../components/collective-navbar/index';
import { webpackCollective } from '../mocks/collectives';
import { useState } from 'react';

<Meta title="Design system/CollectiveNavbar" component={CollectiveNavbar} />

<Canvas>
  <Story name="Default">
    {() => {
      const [selected, setSelected] = useState('CONTRIBUTE');
      return (
        <CollectiveNavbar collective={webpackCollective} selectedCategory={selected} onSectionClick={setSelected} />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Loading state">
    {() => {
      return <CollectiveNavbar isLoading />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Customize CTAs">
    {() => {
      const [selected, setSelected] = useState('CONTRIBUTE');
      return (
        <CollectiveNavbar
          collective={webpackCollective}
          selectedCategory={selected}
          onSectionClick={setSelected}
          callsToAction={{ hasContact: true, hasSubmitExpense: true }}
        />
      );
    }}
  </Story>
</Canvas>
