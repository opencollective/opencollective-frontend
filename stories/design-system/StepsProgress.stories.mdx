import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StepsProgress from '../../components/StepsProgress';

<Meta title="Design system/StepsProgress" component={StepsProgress} />

### Default styling

By default, component take the entire width of its container.

Following example has a resizable container.

<Canvas>
  <Story name="Default">
    {() => {
      const [focus, setFocus] = React.useState(null);
      return (
        <div style={{ resize: 'horizontal', padding: '15px', overflow: 'auto', width: '80%', maxWidth: '95%' }}>
          <StepsProgress
            steps={[{ name: 'BUY POTATOES' }, { name: 'COOK POTATOES' }, { name: 'EAT POTATOES' }]}
            focus={focus}
            onStepSelect={setFocus}
          >
            {({ step }) => <div style={{ textAlign: 'center' }}>{step.name}</div>}
          </StepsProgress>
        </div>
      );
    }}
  </Story>
</Canvas>

### With disabled steps

<Canvas>
  <Story name="Disabled Steps">
    {() => {
      const [focus, setFocus] = React.useState(null);
      return (
        <StepsProgress
          steps={[
            { name: 'BUY POTATOES' },
            { name: 'COOK POTATOES' },
            { name: 'EAT POTATOES' },
            { name: 'THEN START AGAIN' },
          ]}
          disabledStepNames={['THEN START AGAIN']}
          focus={focus}
          onStepSelect={setFocus}
        >
          {({ step }) => <div style={{ textAlign: 'center' }}>{step.name}</div>}
        </StepsProgress>
      );
    }}
  </Story>
</Canvas>

### With a step loading

<Canvas>
  <Story name="Step Loading">
    {() => {
      const [focus, setFocus] = React.useState(null);
      return (
        <StepsProgress
          steps={[{ name: 'a' }, { name: 'b' }, { name: 'c' }, { name: 'd' }, { name: 'e' }, { name: 'f' }]}
          loadingStep={{ name: 'c' }}
          focus={focus}
          onStepSelect={setFocus}
        />
      );
    }}
  </Story>
</Canvas>

### All completed

<Canvas>
  <Story name="All Completed">
    {() => {
      const [focus, setFocus] = React.useState(null);
      return (
        <StepsProgress
          steps={[{ name: 'BUY POTATOES' }, { name: 'COOK POTATOES' }, { name: 'EAT POTATOES' }]}
          onStepSelect={setFocus}
          allCompleted
        />
      );
    }}
  </Story>
</Canvas>
