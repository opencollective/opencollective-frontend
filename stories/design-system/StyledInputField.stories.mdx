import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledInputField from '../../components/StyledInputField';
import StyledInput from '../../components/StyledInput';
import theme from '../../lib/theme';

<Meta
  title="Design system/StyledInputField"
  component={StyledInputField}
  argTypes={{
    children: { defaultValue: inputProps => <StyledInput {...inputProps} width="100%" /> },
    label: { defaultValue: 'Field name' },
    name: { defaultValue: 'my-input' },
  }}
  parameters={{
    actions: {
      handles: ['onChange'],
    },
  }}
/>

# StyledInputField

## Default

export const DefaultStory = props => <StyledInputField {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />
