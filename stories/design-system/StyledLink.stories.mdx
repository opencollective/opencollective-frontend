import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledLink from '../../components/StyledLink';

<Meta title="Design system/StyledLink" component={StyledLink} />

Default styling:

<Canvas>
  <Story name="Default Styling">
    {() => {
      return <StyledLink>Default link</StyledLink>;
    }}
  </Story>
</Canvas>

This component can also be displayed as a button using the theme variants.

Below is an example of the `primary` + `medium` styling. See [`StyledButton`](#styledbutton) for more examples:

<Canvas>
  <Story name="As a Button">
    {() => {
      return (
        <StyledLink buttonStyle="primary" buttonSize="medium">
          Button Link
        </StyledLink>
      );
    }}
  </Story>
</Canvas>
