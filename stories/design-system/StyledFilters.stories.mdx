import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledFilters from '../../components/StyledFilters';

<Meta
  title="Design system/StyledFilters"
  component={StyledFilters}
  argTypes={{
    filters: { defaultValue: ['Option 1', 'Option 2', 'Option 3'] },
    selected: { control: { disable: true } },
    onChange: { action: 'onChange' },
  }}
  args={{
    filters: ['Option 1', 'Option 2', 'Option 3'],
  }}
/>

# StyledFilters

## Default

export const DefaultStory = props => {
  const [selected, setSelected] = React.useState('Option 1');
  return <StyledFilters {...props} selected={selected} onChange={setSelected} />;
};

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />

## MultiSelect

export const MultiSelectStory = props => {
  const [selected, setSelected] = React.useState([]);
  return <StyledFilters {...props} selected={selected} onChange={setSelected} />;
};

<Story name="MultiSelect">{MultiSelectStory.bind({})}</Story>
