import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import MessageBox from '../../components/MessageBox';
import { Flex } from '../../components/Grid';
import Image from '../../components/Image';

<Meta
  title="Design system/MessageBox"
  component={MessageBox}
  argTypes={{
    type: { options: ['white', 'info', 'success', 'warning', 'error'], control: { type: 'radio' } },
    withIcon: { control: { type: 'boolean' } },
    isLoading: { control: { type: 'boolean' } },
  }}
/>

# MessageBox

## Default

export const DefaultStory = props => (
  <MessageBox {...props}>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</MessageBox>
);

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />

## Types

<Canvas>
  <Story name="Types" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox type="white">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="info">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="success">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="warning">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="error">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
    </Flex>
  </Story>
</Canvas>

## With loading spinner

<Canvas>
  <Story name="With loading spinner" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox withIcon isLoading type="white">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon isLoading type="info">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon isLoading type="success">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon isLoading type="warning">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon isLoading type="error">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>

## With icons

<Canvas>
  <Story name="With icons" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox type="white" withIcon>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox type="info" withIcon>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="success" withIcon>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="warning" withIcon>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="error" withIcon>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>
