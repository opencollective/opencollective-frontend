import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import MessageBox from '../../components/MessageBox';
import { Flex } from '../../components/Grid';

<Meta
  title="Design system/MessageBox"
  component={MessageBox}
  argTypes={{
    type: { options: ['white', 'dark', 'info', 'success', 'warning', 'error'], control: { type: 'radio' } },
    withIcon: { control: { type: 'boolean' } },
    isLoading: { control: { type: 'boolean' } },
  }}
/>

# MessageBox

## Default

export const DefaultStory = props => (
  <MessageBox {...props}>Lorem ipsum dolor sit amet, consectetur adipiscing elit.</MessageBox>
);

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />

## Types

<Canvas>
  <Story name="Types" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox withIcon type="dark">
        Dark
      </MessageBox>
      <MessageBox type="white">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="info">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="success">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="warning">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
      <MessageBox type="error">You must be signed in as an admin or the expense submitter to read them.</MessageBox>
    </Flex>
  </Story>
</Canvas>

## With loading spinner

<Canvas>
  <Story name="With loading spinner" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox isLoading type="dark">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox isLoading type="white">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox isLoading type="info">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox isLoading type="success">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox isLoading type="warning">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox isLoading type="error">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>

## With icons

<Canvas>
  <Story name="With icons" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox withIcon type="info">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="success">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="warning">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="error">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>

## With title

<Canvas>
  <Story name="With title" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox withIcon type="info" title="These comments are private">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="success" title="These comments are private">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="warning" title="These comments are private">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="error" title="These comments are private">
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>

## With action

<Canvas>
  <Story name="With action" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <Flex flexDirection="column" gridGap="20px" px={2} maxWidth="800px">
      <MessageBox withIcon type="info" title="These comments are private" action={<a href="#">Sign in</a>}>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="success" title="These comments are private" action={<a href="#">Sign in</a>}>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="warning" title="These comments are private" action={<a href="#">Sign in</a>}>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
      <MessageBox withIcon type="error" title="These comments are private" action={<a href="#">Sign in</a>}>
        You must be signed in as an admin or the expense submitter to read them.
      </MessageBox>
    </Flex>
  </Story>
</Canvas>
