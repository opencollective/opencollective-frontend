import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledAmountPicker from '../../components/StyledAmountPicker';

<Meta title="Design system/StyledAmountPicker" component={StyledAmountPicker} />

### Base

<Canvas>
  <Story name="Base">
    {() => {
      const [value, setValue] = React.useState(5000);
      return (
        <StyledAmountPicker currency="USD" presets={[500, 5000, 10000, 50000]} value={value} onChange={setValue} />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Base (with EUR)">
    {() => {
      const [value, setValue] = React.useState(5000);
      return (
        <StyledAmountPicker currency="EUR" presets={[500, 5000, 10000, 50000]} value={value} onChange={setValue} />
      );
    }}
  </Story>
</Canvas>

### With a min amount

<Canvas>
  <Story name="With Min Amount">
    {() => {
      const [value, setValue] = React.useState(5000);
      return (
        <StyledAmountPicker
          currency="USD"
          presets={[500, 5000, 10000, 50000]}
          value={value}
          onChange={setValue}
          min={500}
        />
      );
    }}
  </Story>
</Canvas>

### Without presets

<Canvas>
  <Story name="Without presets">
    {() => {
      const [value, setValue] = React.useState(5000);
      return <StyledAmountPicker currency="USD" value={value} onChange={setValue} />;
    }}
  </Story>
</Canvas>
