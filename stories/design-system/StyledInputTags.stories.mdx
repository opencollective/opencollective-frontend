import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledInputTags from '../../components/StyledInputTags';
import theme from '../../lib/theme';

<Meta
  title="Design system/StyledInputTags"
  component={StyledInputTags}
  argTypes={{
    defaultValue: { defaultValue: ['OPEN', 'SOURCE', 'RULES'] },
    suggestedTags: { defaultValue: ['BLUE', 'ORANGE', 'PINK'] },
  }}
  parameters={{
    actions: {
      handles: ['onChange'],
    },
  }}
/>

# StyledInputTags

## Default

export const DefaultStory = props => <StyledInputTags {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />
