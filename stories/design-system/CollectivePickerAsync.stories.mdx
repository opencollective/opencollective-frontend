import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import CollectivePickerAsync from '../../components/CollectivePickerAsync';
import { randomCollectivesList } from '../mocks/collectives';

<Meta title="Design system/CollectivePickerAsync" component={CollectivePickerAsync} />

### Default component

> Try to search:
>
> - Any word set in name/slug/description
> - Emails

<Canvas>
  <Story name="Default">
    {() => {
      return <CollectivePickerAsync collectives={randomCollectivesList} inputId="collective-picker-storybook" />;
    }}
  </Story>
</Canvas>

### Specialized for users

<Canvas>
  <Story name="User Type">
    {() => {
      return (
        <CollectivePickerAsync
          collectives={randomCollectivesList}
          types={['USER']}
          inputId="collective-picker-storybook"
        />
      );
    }}
  </Story>
</Canvas>

### Specialized for collectives

<Canvas>
  <Story name="Collective Type">
    {() => {
      return (
        <CollectivePickerAsync
          collectives={randomCollectivesList}
          types={['COLLECTIVE']}
          inputId="collective-picker-storybook"
        />
      );
    }}
  </Story>
</Canvas>

### Specialized for organizations

<Canvas>
  <Story name="Organization Type">
    {() => {
      return (
        <CollectivePickerAsync
          collectives={randomCollectivesList}
          types={['ORGANIZATION']}
          inputId="collective-picker-storybook"
        />
      );
    }}
  </Story>
</Canvas>

### Specialized for events

<Canvas>
  <Story name="Event Type">
    {() => {
      return (
        <CollectivePickerAsync
          collectives={randomCollectivesList}
          types={['EVENT']}
          inputId="collective-picker-storybook"
        />
      );
    }}
  </Story>
</Canvas>
