import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import CreateProfileV2 from '../../components/CreateProfileV2';

<Meta title="Design system/CreateProfileV2" component={CreateProfileV2} />

<Canvas>
  <Story name="Default">
    {() => {
      const [action, setAction] = React.useState(null);
      const [email, setEmail] = React.useState('');
      return (
        <div>
          <CreateProfileV2
            onSecondaryAction={() => setAction('Secondary action button was clicked!')}
            onEmailChange={setEmail}
            email={email}
          />
          {action && <p>{action}</p>}
        </div>
      );
    }}
  </Story>
</Canvas>

Displays passed in errors:

<Canvas>
  <Story name="Error Passing">
    {() => {
      const [action, setAction] = React.useState(null);
      const [email, setEmail] = React.useState('');
      return (
        <div>
          <CreateProfileV2
            onSecondaryAction={() => setAction('Secondary action button was clicked!')}
            onEmailChange={setEmail}
            email={email}
            errors={{ email: 'email already exists' }}
          />
          {action && <p>{action}</p>}
        </div>
      );
    }}
  </Story>
</Canvas>
