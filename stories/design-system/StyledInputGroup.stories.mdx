import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledInputGroup from '../../components/StyledInputGroup';
import StyledInput from '../../components/StyledInput';
import theme from '../../lib/theme';

<Meta
  title="Design system/StyledInputGroup"
  component={StyledInputGroup}
  argTypes={{
    prepend: { defaultValue: 'https://' },
    type: { defaultValue: 'url' },
  }}
  parameters={{
    actions: {
      handles: ['onChange'],
    },
  }}
/>

# StyledInputGroup

## Default

export const DefaultStory = props => <StyledInputGroup {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />
