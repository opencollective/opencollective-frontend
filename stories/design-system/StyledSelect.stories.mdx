import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledSelect from '../../components/StyledSelect';
import theme from '../../lib/theme';

<Meta
  title="Design system/StyledSelect"
  component={StyledSelect}
  argTypes={{
    options: { defaultValue: Array.from({ length: 50 }, (x, idx) => ({ label: `Option ${idx}`, value: idx })) },
    isLoading: { control: { type: 'boolean' } },
    disabled: { control: { type: 'boolean' } },
    error: { control: { type: 'boolean' } },
    isSearchable: { control: { type: 'boolean' }, defaultValue: true },
    onChange: { action: 'onChange' },
  }}
/>

# StyledSelect

## Default

export const DefaultStory = props => <StyledSelect {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />
