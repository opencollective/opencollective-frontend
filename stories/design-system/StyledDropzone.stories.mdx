import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import StyledDropzone from '../../components/StyledDropzone';
import ToastProvider from '../../components/ToastProvider';
import { UserContext } from '../../components/UserProvider';
import { loggedInUser } from '../mocks/loggedInUser';

<Meta title="Design system/StyledDropzone" component={StyledDropzone} />

## Default

<Canvas>
  <Story name="Default">
    {() => (
      <UserContext.Provider value={{ LoggedInUser: loggedInUser, loadingLoggedInUser: false }}>
        <ToastProvider>
          <StyledDropzone onSuccess={console.log} />;
        </ToastProvider>
      </UserContext.Provider>
    )}
  </Story>
</Canvas>

## Customize message

### Additional message

<Canvas>
  <Story name="Additional Message">
    {() => (
      <UserContext.Provider value={{ LoggedInUser: loggedInUser, loadingLoggedInUser: false }}>
        <ToastProvider>
          <StyledDropzone onSuccess={console.log}>Hello World</StyledDropzone>;
        </ToastProvider>
      </UserContext.Provider>
    )}
  </Story>
</Canvas>

## Custom size

<Canvas>
  <Story name="Custom Size">
    {() => (
      <UserContext.Provider value={{ LoggedInUser: loggedInUser, loadingLoggedInUser: false }}>
        <ToastProvider>
          <StyledDropzone onSuccess={console.log} size={150} />;
        </ToastProvider>
      </UserContext.Provider>
    )}
  </Story>
</Canvas>

## Loading state

<Canvas>
  <Story name="Loading State">
    {() => (
      <UserContext.Provider value={{ LoggedInUser: loggedInUser, loadingLoggedInUser: false }}>
        <ToastProvider>
          <StyledDropzone onSuccess={console.log} size={150} isLoading />;
        </ToastProvider>
      </UserContext.Provider>
    )}
  </Story>
</Canvas>

### Show progress

<Canvas>
  <Story name="Show Progress">
    {() => (
      <UserContext.Provider value={{ LoggedInUser: loggedInUser, loadingLoggedInUser: false }}>
        <ToastProvider>
          <StyledDropzone onSuccess={console.log} isLoading loadingProgress={100} />;
        </ToastProvider>
      </UserContext.Provider>
    )}
  </Story>
</Canvas>
