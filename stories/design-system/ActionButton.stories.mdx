import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import ActionButton from '../../components/ActionButton';
import theme from '../../lib/theme';

<Meta
  title="Design system/ActionButton"
  component={ActionButton}
  argTypes={{
    children: { defaultValue: 'Click me!' },
    buttonSize: { options: Object.keys(theme.buttonSizes), control: { type: 'radio' } },
  }}
  parameters={{
    actions: {
      handles: ['mouseover', 'click'],
    },
  }}
/>

# ActionButton

## Default

export const DefaultStory = props => <ActionButton {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />

## Variants

<Canvas>
  <Story name="Variants" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <ActionButton mr={2}>Normal</ActionButton>
    <ActionButton mr={2} isSecondary>
      Secondary
    </ActionButton>
  </Story>
</Canvas>

## Sizes

<Canvas>
  <Story name="Button sizes" parameters={{ controls: { hideNoControlsWarning: true } }}>
    <ActionButton mr={2} buttonSize="xLarge">
      Extra Large
    </ActionButton>
    <ActionButton mr={2} buttonSize="large">
      Large
    </ActionButton>
    <ActionButton mr={2} buttonSize="medium">
      Medium
    </ActionButton>
    <ActionButton mr={2} buttonSize="small">
      Small
    </ActionButton>
    <ActionButton mr={2} buttonSize="tiny">
      Tiny
    </ActionButton>
  </Story>
</Canvas>
