import { Story, Canvas, Meta } from '@storybook/addon-docs/blocks';
import AccountPicker from '../../components/AccountPicker';
import { randomCollectivesList } from '../mocks/collectives';

<Meta title="Design system/AccountPicker" component={AccountPicker} />

## Default

<Canvas>
  <Story name="Default">
    {() => {
      const [collective, setCollective] = React.useState(null);
      return <AccountPicker onChange={setCollective} value={collective} accounts={randomCollectivesList.slice(0, 6)} />;
    }}
  </Story>
</Canvas>

## Searchable

<Canvas>
  <Story name="Searchable">
    {() => {
      const [collective, setCollective] = React.useState(null);
      return <AccountPicker onChange={setCollective} value={collective} accounts={randomCollectivesList} searchable />;
    }}
  </Story>
</Canvas>

## Grouped

<Canvas>
  <Story name="Grouped">
    {() => {
      const [collective, setCollective] = React.useState(null);
      return <AccountPicker onChange={setCollective} value={collective} accounts={randomCollectivesList} groupped />;
    }}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled">
    <AccountPicker disabled />
  </Story>
</Canvas>
