import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import DebitCreditList, { DebitItem, CreditItem } from '../../components/budget/DebitCreditList';

<Meta title="Design system/DebitCreditList" component={DebitCreditList} />

<Canvas>
  <Story name="Default">
    {() => {
      return (
        <DebitCreditList>
          {[1, 2, 3, 4, 5].map(i =>
            i % 2 === 0 ? (
              <CreditItem key={i}>
                <div style={{ height: 30 * (i + 1), display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
                  I'm {i}. I'm a credit!
                </div>
              </CreditItem>
            ) : (
              <DebitItem key={i}>
                <div style={{ height: 30 * (i + 1), display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
                  I'm {i}. I'm a debit!
                </div>
              </DebitItem>
            ),
          )}
        </DebitCreditList>
      );
    }}
  </Story>
</Canvas>
