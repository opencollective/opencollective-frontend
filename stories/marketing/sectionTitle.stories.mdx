import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { SectionTitle } from '../../components/marketing/Text';
import { UserContext } from '../../components/UserProvider';

<Meta title="Marketing/SectionTitle" component={SectionTitle} />

# Marketing

## Default

export const SectionTitleStory = props => <SectionTitle {...props}>Raise money with full transparency</SectionTitle>;

<Story name="Default">{SectionTitleStory.bind({})}</Story>
