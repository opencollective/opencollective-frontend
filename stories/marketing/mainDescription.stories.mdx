import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { MainDescription } from '../../components/marketing/Text';
import { UserContext } from '../../components/UserProvider';

<Meta title="Marketing/MainDescription" component={MainDescription} />

# Marketing

## Default

export const MainDescriptionStory = props => (
  <MainDescription {...props}>
    Doohi Collective is a legal and financial toolbox for groups. It’s a fundraising + legal status + money management
    platform for your community. What do you want to do?
  </MainDescription>
);

<Story name="Default">{MainDescriptionStory.bind({})}</Story>
