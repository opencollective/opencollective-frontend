import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { SectionDescription } from '../../components/marketing/Text';
import { UserContext } from '../../components/UserProvider';

<Meta title="Marketing/SectionDescription" component={SectionDescription} />

# Marketing

## Default

export const SectionDescriptionStory = props => (
  <SectionDescription {...props}>We do the paperwork so you can focus on your mission.</SectionDescription>
);

<Story name="Default">{SectionDescriptionStory.bind({})}</Story>
