import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PayoutMethodData from '../../components/expenses/PayoutMethodData';
import { payoutMethodPaypal, payoutMethodBankAccount, payoutMethodOther } from '../mocks/payout-methods';

<Meta title="Expenses/PayoutMethodData" component={PayoutMethodData} />

<Canvas>
  <Story name="PayPal">
    {() => {
      return <PayoutMethodData payoutMethod={payoutMethodPaypal} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Bank">
    {() => {
      return <PayoutMethodData payoutMethod={payoutMethodBankAccount} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Other">
    {() => {
      return <PayoutMethodData payoutMethod={payoutMethodOther} />;
    }}
  </Story>
</Canvas>
