import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import ExpenseTypeRadioSelect from '../../components/expenses/ExpenseTypeRadioSelect';
import expenseTypes from '../../lib/constants/expenseTypes';

<Meta
  title="Expenses/ExpenseTypeRadioSelect"
  component={ExpenseTypeRadioSelect}
  argTypes={{
    supportedExpenseTypes: {
      defaultValue: [expenseTypes.RECEIPT, expenseTypes.INVOICE, expenseTypes.GRANT],
    },
    value: {
      options: Object.values(expenseTypes),
      control: { type: 'radio' },
    },
  }}
  args={{
    supportedExpenseTypes: [expenseTypes.RECEIPT, expenseTypes.INVOICE, expenseTypes.GRANT],
  }}
  parameters={{
    actions: {
      handles: ['onChange'],
    },
  }}
/>

## Default

export const DefaultStory = props => <ExpenseTypeRadioSelect {...props} />;

<Story name="Default">{DefaultStory.bind({})}</Story>

<ArgsTable story="Default" />
