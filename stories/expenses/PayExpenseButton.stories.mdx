import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import PayExpenseButton from '../../components/expenses/PayExpenseButton';
import { webpackCollective, openSourceHost } from '../mocks/collectives';

<Meta title="Expenses/PayExpenseButton" component={PayExpenseButton} />

<Canvas>
  <Story name="Base (PayPal)">
    {() => {
      return (
        <PayExpenseButton
          onSubmit={console.log}
          expense={{ id: '1', amount: 4200, payoutMethod: { type: 'PAYPAL' } }}
          collective={webpackCollective}
          host={openSourceHost}
        />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Base (Transferwise)">
    {() => {
      return (
        <PayExpenseButton
          onSubmit={console.log}
          expense={{ id: '1', amount: 4200, payoutMethod: { type: 'BANK_ACCOUNT' } }}
          collective={webpackCollective}
          host={openSourceHost}
        />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Base (Other)">
    {() => {
      return (
        <PayExpenseButton
          onSubmit={console.log}
          expense={{ id: '1', amount: 4200, payoutMethod: { type: 'OTHER' } }}
          collective={webpackCollective}
          host={openSourceHost}
        />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled (Manually)">
    {() => {
      return (
        <PayExpenseButton
          onSubmit={console.log}
          expense={{ id: '1', amount: 4200, payoutMethod: { type: 'PAYPAL' } }}
          collective={webpackCollective}
          host={openSourceHost}
          disabled
        />
      );
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Disabled (Not enough funds)">
    {() => {
      return (
        <PayExpenseButton
          onSubmit={console.log}
          expense={{ id: '1', amount: 12000, payoutMethod: { type: 'PAYPAL' } }}
          collective={{ ...webpackCollective, balance: 500 }}
          host={openSourceHost}
        />
      );
    }}
  </Story>
</Canvas>
