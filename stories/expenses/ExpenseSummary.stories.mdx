import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import ExpenseSummary from '../../components/expenses/ExpenseSummary';
import { expenseReceipt, expenseInvoice } from '../mocks/expenses-v2';
import { openSourceHost } from '../mocks/collectives';

<Meta title="Expenses/ExpenseSummary" component={ExpenseSummary} />

<Canvas>
  <Story name="Invoice">
    {() => {
      return <ExpenseSummary host={openSourceHost} expense={expenseReceipt} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Receipt">
    {() => {
      return <ExpenseSummary host={openSourceHost} expense={expenseInvoice} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Loading">
    {() => {
      const [isLoading, setLoading] = React.useState(true);
      return (
        <div>
          <button onClick={() => setLoading(!isLoading)}>Toggle loading</button>
          <br />
          <hr />
          <br />
          <ExpenseSummary host={openSourceHost} expense={expenseInvoice} isLoading={isLoading} />
        </div>
      );
    }}
  </Story>
</Canvas>
