import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import ExpenseSummary from '../../components/expenses/ExpenseSummary';
import { expenseReceipt, expenseInvoice } from '../mocks/expenses-v2';
import { webpackCollective } from '../mocks/collectives';

<Meta title="Expenses/ExpenseSummary" component={ExpenseSummary} />

<Canvas>
  <Story name="Invoice">
    {() => {
      return <ExpenseSummary collective={webpackCollective} expense={expenseReceipt} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Receipt">
    {() => {
      return <ExpenseSummary collective={webpackCollective} expense={expenseInvoice} />;
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Loading">
    {() => {
      return (
        <ExpenseSummary isLoading={true} />
      );
    }}

  </Story>
</Canvas>
