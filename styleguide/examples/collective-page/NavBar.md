```js
initialState = { selected: 'join-us' };
<div style={{ height: 56 }}>
  <NavBar selected={state.selected} onSectionClick={selected => setState({ selected })} linkBuilder={() => null} />
</div>;
```
