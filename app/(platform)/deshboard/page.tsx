import React from 'react';
import { redirect } from 'next/navigation';

export default function DeshboardRootPage() {
  // Redirect to a default dashboard or show a landing page
  // For now, let's redirect to a default collective or show a selection page
  return <div>Root dashboard</div>; // or wherever you want to redirect users who hit /deshboard directly
}
