general:
  branches:
    ignore:
      - staging
      - production
dependencies:
  pre:
    - ./scripts/setup_circleci.sh
  cache_directories:
    - "~/cache"
test:
  post:
    - npm run semantic-release
machine:
  node:
    version: 8.4.0
  environment:
    NODE_ENV: circleci
