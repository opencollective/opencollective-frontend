general:
  branches:
    ignore:
      - staging
      - production
dependencies:
  pre:
    - ./scripts/setup_circleci.sh
  cache_directories:
    - "~/cache"
machine:
  node:
    version: 10.2.1
  environment:
    NODE_ENV: circleci
